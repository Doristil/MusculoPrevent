# Archive historique

`Base_de_donnees.csv` est conservé uniquement pour retrouver l'origine du
premier prototype. Il n'est pas importé par l'application et ne constitue plus
une source de référence.

Le mémoire Word validé est le référentiel unique du catalogue actif.
